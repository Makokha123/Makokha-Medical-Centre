# Makokha Medical Centre - Environment Variables (TEMPLATE)
#
# Copy this file to `.env` for local development and fill in values.
# IMPORTANT:
# - Do NOT commit your real `.env` file.
# - Do NOT paste real secrets into tickets/chats.

# --- Environment / runtime ---
# Use one of: development | production
ENV=development
FLASK_ENV=development
FLASK_DEBUG=false

# Only set these in real production deployments.
# PRODUCTION=true
# RENDER=1

# Trust reverse proxy headers when behind Nginx/Render/etc.
TRUST_PROXY=true

# Enable HSTS (recommended only when HTTPS is guaranteed)
# ENABLE_HSTS=true

# --- Core secrets (REQUIRED in production) ---
# Generate long random values.
SECRET_KEY=change-me
SECURITY_PASSWORD_SALT=change-me

# Fernet key (32 url-safe base64 bytes). Example generation:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=change-me

# Separate key for encrypted backups (recommended)
BACKUP_ENCRYPTION_KEY=change-me

# Optional: comma-separated older Fernet keys for decrypt-only during rotation
# LEGACY_FERNET_KEYS=

# --- Database ---
# Local default is sqlite:///instance/clinic.db if omitted.
# For Postgres:
# DATABASE_URL=postgresql+psycopg2://user:password@host:5432/dbname
DATABASE_URL=

# If you explicitly want SQLite fallback on platforms, set this (usually not needed)
# SQLITE_FALLBACK_URL=sqlite:///instance/clinic.db

# --- Email (Brevo/SMTP) ---
MAIL_SERVER=smtp-relay.brevo.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=

# Alternative variable names supported by config.py
# BREVO_EMAIL=
# BREVO_SMTP_KEY=

# --- Rate limiting (Redis recommended for multi-worker production) ---
# REDIS_URL=redis://:password@host:6379/0
# RATELIMIT_STORAGE_URI=redis://:password@host:6379/0

# --- AI (DeepSeek / OpenAI-compatible client) ---
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_TIMEOUT=30
DEEPSEEK_MAX_RETRIES=3

# --- Gunicorn (production) ---
# WEB_CONCURRENCY=2
# GUNICORN_WORKER_CLASS=gthread
# GUNICORN_THREADS=4
# TIMEOUT=60
# GRACEFUL_TIMEOUT=30
# LOG_LEVEL=info
# PRELOAD_APP=false

# --- One-time seeding (local/dev only) ---
# To create an initial admin account in local/dev:
# SEED_DEFAULT_USERS=true
# SEED_ADMIN_USERNAME=Admin
# SEED_ADMIN_EMAIL=admin@example.com
# SEED_ADMIN_PASSWORD=change-me

# --- Optional backups to S3 (if enabled in your deployment) ---
AWS_BACKUP_BUCKET=
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
